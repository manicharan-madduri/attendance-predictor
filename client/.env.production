# Production environment — used by Vite during 'npm run build'
# Replace the URL below with your actual Render backend URL after deploying Step 3
VITE_API_URL=https://your-render-app.onrender.com/api
